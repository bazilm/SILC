begin
decl
string names[10];
int ages[10],i,n;
enddecl
write("Enter the number of names\n");
read(n);
i=0;
write("Enter your names\n");
while(i<n) do
	read(names[i]);
endwhile;

i=0;
while(i<n) do
	write(names[i]);

endwhile;
end
