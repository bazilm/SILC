#include<stdbool.h>
#include<stdarg.h>

typedef enum  {CONSTANT,OPERATOR,VARIABLE} NodeType ;

struct returnType
{
int value;
bool printable;
};

typedef struct 
{
int value;
}ConNode;

typedef struct 
{
int id;
}VarNode;

typedef struct 
{
int op;
int nops;
struct NodeTag *operands;
}OperNode;

typedef struct NodeTag
{
NodeType nodeType;

union
{
ConNode con;
OperNode oper;
VarNode var;
};

}Node;

Node * makeConNode(int);
Node * makeVarNode(char);
Node * makeOperNode(int,int,...);
union returnType interpret(Node *);
void freeNode(Node *);

extern int *sym[26];




